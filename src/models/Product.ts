export interface Product {
  id: number;
  name: string;
  description: string;
  price: number;
}

export interface ProductItem {
  id: number;
  productName: string;
  description: string;
  price: number;
}